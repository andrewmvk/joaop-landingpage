import React from "react";

export default function Header() {
   return (
      <nav className={`header-container poppins`}>
         <a href="#about">Sobre mim</a>
         <a href="#occupation">Área de atuação</a>
         <a href="#contact">Entre em contato</a>
      </nav>
   );
}
